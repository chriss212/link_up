import 'package:flutter/material.dart';

class AppColors {
  // Paleta base del proyecto
  static const lemon  = Color(0xFFFFF085); // fondo/accent suave
  static const peach  = Color(0xFFFCB454); // secundarios/suaves
  static const orange = Color(0xFFFF9B17); // PRIMARY
  static const coral  = Color(0xFFF16767); // alertas/secondary

  // Neutrales 
  static const background = lemon;         // fondo app
  static const surface    = Colors.white;  // tarjetas, inputs
  static const textDark   = Color(0xFF1F2430);
  static const textLight  = Colors.white;
}
